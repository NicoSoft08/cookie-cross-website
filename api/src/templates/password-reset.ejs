<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #fcfcfc;
            padding: 20px;
            color: #333;
        }

        .email-container {
           max-width: 600px;
            margin: 0 auto;
            background-color: #fcfcfc;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: linear-gradient(135deg, #417abc, #8cb7df);
            padding: 20px;
            text-align: center;
            color: white;
        }

        .content {
            padding: 30px;
        }

        .content p {
            margin-bottom: 15px;
        }

        .reset-button {
            display: inline-block;
            background-color: #417abc;
            color: white;
            text-decoration: none;
            padding: 12px 20px;
            font-weight: bold;
            border-radius: 6px;
            margin-top: 15px;
        }

        .reset-link-box {
            background-color: #f1f5f9;
            padding: 10px;
            border-left: 4px solid #417abc;
            word-break: break-all;
            font-size: 14px;
            margin-top: 10px;
        }

        .footer {
            text-align: center;
            padding: 15px;
            background-color: #6698cd;
            color: white;
            font-size: 12px;
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="header">
            <h1>🔐 Réinitialisation de mot de passe</h1>
        </div>
        <div class="content">
            <p>Bonjour <strong>
                    <%= firstName %>
                        <%= lastName %>
                </strong>,</p>
            <p>Vous avez demandé à réinitialiser votre mot de passe pour votre compte <strong>AdsCity</strong>.</p>
            <p>Veuillez cliquer sur le bouton ci-dessous pour créer un nouveau mot de passe :</p>

            <p style="text-align: center;">
                <a href="<%= resetLink %>" class="reset-button">Réinitialiser mon mot de passe</a>
            </p>

            <p>Si le bouton ne fonctionne pas, copiez-collez ce lien dans votre navigateur :</p>
            <div class="reset-link-box">
                <%= resetLink %>
            </div>

            <p>Ce lien expirera dans <%= expirationTime %> heures.</p>

            <p>Si vous n'avez pas fait cette demande, ignorez simplement cet e-mail.</p>
            <p>Cordialement,<br>L'équipe AdsCity</p>
        </div>

        <div class="footer">
            © <%= year %> AdsCity – Tous droits réservés. Cet email a été envoyé à <%= email %>
        </div>
    </div>

</body>

</html>